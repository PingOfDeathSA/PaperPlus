<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to the Research Recommendations System</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100%;
      background-image: url('/res.jpg');
      background-repeat: no-repeat;
      background-size: cover;
      filter: blur(0px);
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      background-color: #e0e0e0;
      opacity: 0.9;
      padding: 40px;
      text-align: center;
    
   
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .container2{
      margin: 0 auto;
      padding: 40px;
      text-align: center;
      background-color: #e0e0e0;
      opacity: 0.6;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }


    
h1 {
  color: #ffffff;
  text-shadow: 2px 2px 4px #4b4b4b;
}
h4{
  color: #ffffff;
  text-shadow: 2px 2px 4px #4b4b4b;
}

    p {
      color: #000000;
      margin-bottom: 20px;
      font-size: large;
      font-weight: bold;
    }

    .btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: #333;
      color: #fff;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s;
    }
    h4{

      color: #e7bbbb;
      font-weight: bold;
        text-shadow: 2px 2px 4px #4b4b4b;
    }
    .btn:hover {
      background-color: #555;
    }
    .welcome{
      text-shadow: 2px 2px 4px #919191;
    }
  </style>

      <!-- Required meta tags -->
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  
      <!-- Bootstrap CSS -->
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  
      <title>Mail App</title>
</head>

<body>
  <div class="container2">
    <h1 >Welcome to PaperPlus</h1>
    <p class="welcome">Thank you for registering. Please configure your interests to receive personalized research paper recommendations.</p>
   
  </div>

  <div class="container">

    <form action="/welcomeUpade.html" method="post">

      <div class="row">
        <div class="col-6 col-md-3">
          <%- include("interest1.ejs") -%>
        </div>
        <div class="col-6 col-md-3">
          <%- include("interest2.ejs") -%>
        </div>
        <div class="col-6 col-md-3"><%- include("interest3.ejs") -%></div>
      </div>

  <div style="width: 100px;"> <button type="submit" class="btn btn-danger">Submit</button></div>
 
</form>
     </div>

     <script>
      // Example starter JavaScript for disabling form submissions if there are invalid fields
      (function () {
        'use strict';
        window.addEventListener('load', function () {
          // Fetch all the forms we want to apply custom Bootstrap validation styles to
          var forms = document.getElementsByClassName('needs-validation');
          // Loop over them and prevent submission
          var validation = Array.prototype.filter.call(forms, function (form) {
            form.addEventListener('submit', function (event) {
              if (form.checkValidity() === false) {
                event.preventDefault();
                event.stopPropagation();
              }
              form.classList.add('was-validated');
            }, false);
          });
        }, false);
      })();
    </script>
      <script>
        // Function to populate the dropdown with options
        function populateDropdown(dropdownId) {
          const dropdown = document.getElementById(dropdownId);
      
          options.forEach(function(option) {
            const optionElement = document.createElement("option");
            optionElement.value = option;
            optionElement.textContent = option;
            dropdown.appendChild(optionElement);
          });
        }
      
        // Call the function for each dropdown
        populateDropdown("researchPapers1");
        populateDropdown("researchPapers2");
        populateDropdown("researchPapers3");
      
        // Event listener for dropdown change
        document.querySelectorAll("select[id^='researchPapers']").forEach(function(dropdown) {
          dropdown.addEventListener("change", function() {
            const selectedValue = this.value;
            console.log("Selected research paper:", selectedValue);
            // Perform further actions based on the selected value
          });
        });
      </script>
       
</body>

</html>
